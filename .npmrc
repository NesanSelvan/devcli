# pnpm 11 runs a strict dep-status check before `pnpm dev`/`build`; it trips on
# esbuild's skipped postinstall (which the build doesn't actually need). Disable it
# so tauri's beforeDevCommand can run.
verify-deps-before-run=false
